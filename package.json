{
  "name": "restaurant-app-root",
  "version": "1.0.0",
  "description": "Root package.json to help Vercel and Render deploy easily",
  "main": "backend/index.js",
  "scripts": {
    "postinstall": "cd backend && npm install",
    "build": "npm run build --prefix backend && npm run build --prefix frontend && node -e \"const fs = require('fs'); fs.cpSync('frontend/dist', 'dist', { recursive: true });\"",
    "start": "node --experimental-specifier-resolution=node backend/index.js"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cookie-parser": "^1.4.6",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.3.2",
    "multer": "^1.4.5-lts.1",
    "nodemailer": "^9.0.5",
    "razorpay": "^2.9.8",
    "ws": "^8.16.0"
  },
  "engines": {
    "node": ">=18.0.0 <23.0.0"
  }
}
