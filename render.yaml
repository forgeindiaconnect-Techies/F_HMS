services:
  - type: web
    name: f-hms
    env: node
    rootDir: backend
    buildCommand: npm install && npm run build
    startCommand: node dist/server.cjs
    envVars:
      - key: NODE_VERSION
        value: 20.18.0
